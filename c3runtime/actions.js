C3.Plugins.GltfStatic.Acts =
    {
        LoadModel(url) {
            this._loadModel(url);
        },
        SetRotation(x, y, z) {
            this._setRotation(x, y, z);
        }
    };
export {};
